# Git LFS files in the media folder.
media/* filter=lfs diff=lfs merge=lfs -text

# Mark the jupytext text as notebooks.
notebooks/*.py linguist-language=Jupyter Notebook
tests/*.py linguist-language=Jupyter Notebook
